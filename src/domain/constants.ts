/**
 * Hằng số domain — mirror của enum trong prisma/schema.prisma (Mục A).
 * Sẽ có test kiểm khớp enum <-> constants khi schema ra đời ở P0-2.
 * Giữ chỗ ở P0-1 để cấu trúc thư mục đúng đặc tả.
 */
export {};
